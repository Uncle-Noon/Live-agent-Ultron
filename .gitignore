# Environment variables
# Users can share their .env template, but should clear personal keys.
# (Removed .env from here as per user request to include it in repo)
# **/.env.local (staying ignored as usual)
**/.env.local

# Node dependencies
node_modules/

# Logs
npm-debug.log*
nodemon-debug.log*

# Debug artifacts
gemini-error.txt

# User data (generated at runtime - do not commit)
**/users/
**/uploads/

# OS files
.DS_Store
Thumbs.db
